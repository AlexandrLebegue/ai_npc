--- a/CrySystem/SystemWin32.cpp
+++ b/CrySystem/SystemWin32.cpp
@@ -1,3 +1,12 @@
+// PS Vita: this file is excluded from the Vita build (see Engine/CrySystem/CMakeLists.txt).
+// Platform-specific system initialisation lives in Platform/PSVita/VitaSystem.cpp instead.
+// The stub below satisfies the linker if this TU is accidentally included.
+#if defined(VITA) || defined(__vita__) || defined(__psp2__)
+// -- Vita stub: nothing to do here --
+#else
+// ── Original Win32 implementation follows ────────────────────────────────
